# OtsukaiList Backend - プロジェクト構成

## 📁 ディレクトリ構造

```
backend/
├── CODING_GUIDELINES.md        # コーディングガイドライン
├── README.md                   # プロジェクト概要
├── build.gradle               # Gradle設定
├── src/main/java/com/atoook/otsukailist/
│   ├── OtsukaiListApplication.java    # Spring Boot メインクラス
│   │
│   ├── controller/            # REST API エンドポイント
│   │   ├── ItemListController.java
│   │   └── ItemController.java
│   │
│   ├── service/               # ビジネスロジック層
│   │   ├── ItemListService.java
│   │   └── ItemService.java
│   │
│   ├── repository/            # データアクセス層
│   │   ├── ItemListRepository.java
│   │   └── ItemRepository.java
│   │
│   ├── model/                 # JPA エンティティ
│   │   ├── ItemList.java
│   │   └── Item.java
│   │
│   ├── dto/                   # データ転送オブジェクト
│   │   ├── ItemListResponse.java
│   │   ├── CreateItemListRequest.java
│   │   ├── ItemResponse.java
│   │   ├── CreateItemRequest.java
│   │   └── UpdateItemRequest.java
│   │
│   ├── mapper/                # Entity ↔ DTO 変換
│   │   ├── ItemListMapper.java
│   │   └── ItemMapper.java
│   │
│   ├── config/                # Spring 設定
│   │   ├── WebConfig.java
│   │   └── DatabaseConfig.java
│   │
│   └── exception/             # 例外処理
│       ├── GlobalExceptionHandler.java
│       └── ResourceNotFoundException.java
│
└── src/test/java/             # テストコード
    └── com/atoook/otsukailist/
        ├── service/
        ├── repository/
        └── controller/
```

## 🎯 各層の責任

### Controller 層

- HTTP リクエスト/レスポンスの処理
- パラメータの検証とマッピング
- HTTP ステータスコードの設定
- 例外ハンドリング

### Service 層

- ビジネスロジックの実装
- トランザクション管理
- セキュリティチェック
- 複数リポジトリの調整

### Repository 層

- データベースアクセス
- クエリ実装
- リスト ID スコープ制限

### Model 層

- データベーステーブル定義
- JPA マッピング
- エンティティ関係定義

### DTO 層

- API インターフェース定義
- 入力検証
- レスポンス形式定義

### Mapper 層

- Entity ↔ DTO 変換
- ビジネスルール適用
- データ変換ロジック

## 🚀 開発フロー

1. **要件定義** → 企画書・設計書更新
2. **API 設計** → DTO・Controller 設計
3. **データ設計** → Entity・Repository 設計
4. **ビジネスロジック** → Service 実装
5. **統合** → Mapper・例外処理
6. **テスト** → 単体・統合テスト
7. **ドキュメント** → ガイドライン更新

## 📚 開発ルール

### ファイル命名規則

- **Controller**: `{リソース名}Controller.java`
- **Service**: `{リソース名}Service.java`
- **Repository**: `{リソース名}Repository.java`
- **Entity**: `{リソース名}.java`
- **DTO**: `{操作}{リソース名}Request.java` / `{リソース名}Response.java`

### パッケージ規則

- 機能別ではなく**層別**にパッケージ分割
- 循環依存の防止
- 明確な依存方向（Controller → Service → Repository）

### テスト配置

- テストは `src/test/java` 以下に本体と同じパッケージ構成
- テストクラス名は `{対象クラス}Test.java`
- 統合テストは `{対象クラス}IntegrationTest.java`
